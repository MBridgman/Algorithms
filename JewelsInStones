/**
 * @param {string} J
 * @param {string} S
 * @return {number}
 */
var numJewelsInStones = function(J, S) {
    var numJewels;
    for (var i = 0; i < S.length; i++){
        if(J.includes(S[i]) == true){
            numJewels++;
        }
    }
    return numJewels;
    
};
